#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@sanity/pkg-utils/bin/pkg-utils.cjs" "$@"
  ret=$?
else 
  node  "$basedir/../@sanity/pkg-utils/bin/pkg-utils.cjs" "$@"
  ret=$?
fi
exit $ret
